
Run npm install
Create a .env file at the root of the file and add the following


NODE_ENV = development
PORT = 5000
MONGO_URI = mongodb+srv://#*##################.abpxecs.mongodb.net/mngmt_db?retryWrites=true&w=majority
 


Add a .gitignore file at the root with the following 

node_modules
.env

All this steps are to be followed
